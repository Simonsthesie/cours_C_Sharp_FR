using System;

/*
 * EXERCICE - Gestion des exceptions
 * 
 * 1. Demande un nombre à l'utilisateur
 * 2. Utilise int.Parse() dans un try
 * 3. En catch (FormatException), affiche "Veuillez entrer un nombre valide"
 * 4. En cas de succès, affiche le double du nombre
 */

class Program
{
    static void Main(string[] args)
    {
        Console.Write("Entrez un nombre : ");
        // Écris le try/catch ici
    }
}
